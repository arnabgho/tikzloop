# Copy to .env and fill in. .env is gitignored; never commit real keys.

# Weights & Biases — enables metric mirroring when running train/run_grpo.py --wandb <project>
WANDB_API_KEY=

# Hugging Face — optional; speeds up downloads and unlocks gated repos
HF_TOKEN=
